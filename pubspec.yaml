name: your_app_name
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  math_expressions: ^2.1.0
  shared_preferences: ^2.2.2  # ← Idinagdag ito

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
